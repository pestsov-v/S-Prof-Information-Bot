const { package, emodji_hand__pointing_down } = require('../helpers/emoji')

const text_sendProduct__query = `Визначте бажаний товар ${package} ниже ${emodji_hand__pointing_down}: `

module.exports = text_sendProduct__query